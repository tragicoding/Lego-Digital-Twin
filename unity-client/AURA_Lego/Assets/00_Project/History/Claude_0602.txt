================================================================
Claude Code 작업 기록
날짜: 2026-06-02
브랜치: feature/unity
================================================================

────────────────────────────────────────────────────────────────
[세션 1] DialogueUI → NPC 머리 위 World Space 말풍선으로 교체
────────────────────────────────────────────────────────────────

## 변경 목적
  가이드 NPC 대사를 화면 고정 UI 대신 NPC 머리 위에 붙는 World UI로 교체

## 수정 파일: Scripts/UI/DialogueUI.cs

  추가된 필드:
    _followTarget   (Transform)  — 추적할 NPC Transform (런타임 주입)
    _heightOffset   (float, 기본 2.2f) — NPC 기준 위쪽 오프셋
    _rightOffset    (float, 기본 0f)   — NPC 기준 오른쪽 오프셋
    _billboardMode  기본값 false → true 변경

  LateUpdate() 변경:
    _followTarget 있으면 매 프레임 위치 추적
      transform.position = _followTarget.position
                         + Vector3.up * _heightOffset
                         + _followTarget.right * _rightOffset

    Billboard: transform.forward = _mainCamera.transform.forward

  추가된 메서드:
    SetFollowTarget(Transform) — NPC 스폰 후 GameFlowManager가 주입

## 수정 파일: Scripts/Managers/GameFlowManager.cs

  OnSessionLoaded() 내 NPC 스폰 직후:
    _dialogueUI?.SetFollowTarget(npc.transform)

## Unity Inspector 작업 (World Space 전환)

  DialogueUI Canvas:
    Render Mode → World Space
    Event Camera → Main Camera
    Scale → (0.01, 0.01, 0.01)
    DialogueUI: Billboard Mode ☑, Height Offset 12~15, Right Offset 1~3

  FreeModePopupUI / MotionPromptUI:
    Render Mode → Screen Space - Overlay (기존 유지)


────────────────────────────────────────────────────────────────
[세션 2] 시그니처 동작 기능 전체 구현
────────────────────────────────────────────────────────────────

## 기능 개요
  - 가이드 모드: 모션 확인 후 "시그니처 동작으로 설정하시겠습니까?" 예/아니오
  - 예 선택 시 SessionData에 저장 + Server Mode면 서버에 PATCH
  - 자유 모드: 이전 관람객 캐릭터에 시그니처 동작 설정
                플레이어 접근 시 자동 루프 재생, 이탈 시 idle 복귀

## 수정/생성 파일 목록

### 1. Scripts/Data/SessionData.cs
  SessionData에 추가:
    public string signature_motion;  // MotionType.ToString(), "" = 미설정

  PlazaSessionData에 추가:
    public string signature_motion;

  요청 DTO 클래스 추가:
    [Serializable] public class BubbleTextRequest   { bubble_text }
    [Serializable] public class SignatureMotionRequest { signature_motion }

### 2. Scripts/Core/ServerConfig.cs
  추가:
    SignatureMotionUrl(sid) → /sessions/{id}/signature-motion

### 3. Scripts/Network/ApiClient.cs
  추가:
    SaveSignatureMotion(sessionId, motionType, onSuccess)
      → PATCH /sessions/{id}/signature-motion
      → SignatureMotionRequest 클래스로 JsonUtility.ToJson() 직렬화

  수정 (문자열 조합 → 클래스 직렬화로 통일):
    UpdateBubbleText: BubbleTextRequest 클래스 사용
    SaveSignatureMotion: SignatureMotionRequest 클래스 사용

### 4. Scripts/UI/SignatureMotionConfirmUI.cs (신규)
  "시그니처 동작으로 설정하시겠습니까?" 예/아니오 다이얼로그
  Show(Action<bool> callback) — 예: callback(true), 아니오: callback(false)
  페이드 인/아웃 CanvasGroup 연출
  Canvas: Screen Space - Overlay

  Hierarchy 구조:
    SignatureMotionConfirmUI (Canvas)
      └── PopupRoot (CanvasGroup, 비활성으로 시작)
           └── Panel
                ├── MessageText (TMP_Text)
                ├── YesButton   (Button — "예")
                └── NoButton    (Button — "아니오")

### 5. Scripts/Character/GuideNPCController.cs
  추가된 이벤트:
    OnSignatureMotionConfirmRequested (Action<string, Action>)
    — (motionInput, onDismissed): 예/아니오 선택 후 onDismissed() 호출

  GuideScenarioRoutine Step 3 변경:
    placedCharacter.PlayMotionFromPrompt(motionInput)
    yield return WaitForSeconds(3f)  (5f → 3f)
    OnSignatureMotionConfirmRequested?.Invoke(motionInput, () => confirmDone = true)
    yield return WaitUntil(() => confirmDone)

### 6. Scripts/Managers/GameFlowManager.cs
  추가된 필드:
    [SerializeField] SignatureMotionConfirmUI _signatureConfirmUI
  추가된 using: LegoTwin.Network

  OnSessionLoaded(): 이벤트 구독 추가
    npc.OnSignatureMotionConfirmRequested += OnSignatureMotionConfirmRequested

  UnsubscribeNPCEvents(): 이벤트 해제 추가

  추가된 핸들러:
    OnSignatureMotionConfirmRequested(motionInput, onDismissed)
      → _signatureConfirmUI.Show(confirmed => ...)
      → confirmed이면 ApplySignatureMotion(motionInput)

  추가된 메서드:
    ApplySignatureMotion(motionInput)
      MotionPromptParser.Parse(motionInput) → MotionType
      session.signature_motion = motionType.ToString()
      Server Mode: ApiClient.Instance.SaveSignatureMotion() 호출

### 7. Scripts/Character/PlacedCharacterController.cs
  추가된 필드:
    _signatureMotionType (MotionType, 기본 Idle)

  추가된 메서드:
    SetupForPlaza(MixamoMotionLibrary, MotionType)
      → motionLibrary, _signatureMotionType 설정
      → _animator.speed = 1f (Awake의 freeze 해제)

    PlaySignatureMotion()
      → _signatureMotionType != Idle이면 루프 재생

    StopSignatureMotion()
      → StopMotionLoop() → idle 복귀

### 8. Scripts/Plaza/PlazaSessionView.cs
  추가된 using: LegoTwin.Character
  추가된 필드: _placedCharacter (PlacedCharacterController)
  추가된 메서드:
    SetCharacter(PlacedCharacterController) — PlazaManager 스폰 후 주입
    PlaySignatureMotion()  → _placedCharacter?.PlaySignatureMotion()
    StopSignatureMotion()  → _placedCharacter?.StopSignatureMotion()

### 9. Scripts/Plaza/LikeSystem.cs
  OnTriggerEnter: GetComponent<PlazaSessionView>()?.PlaySignatureMotion() 추가
  OnTriggerExit:  GetComponent<PlazaSessionView>()?.StopSignatureMotion() 추가

### 10. Scripts/Plaza/PlazaManager.cs
  추가된 using: LegoTwin.Character
  추가된 Inspector 필드:
    characterAnimatorController (RuntimeAnimatorController)
    motionLibrary (MixamoMotionLibrary)
  추가된 런타임 필드:
    _lastSpawnedCharController (PlacedCharacterController)

  SpawnSessionAssets(): 캐릭터 스폰 후 SetupCharacterForPlaza() 호출

  추가된 메서드:
    SetupCharacterForPlaza(go, signatureMotionName)
      → Animator Controller 자동 연결
      → CharacterAnimationController AddComponent
      → PlacedCharacterController AddComponent
      → placed.SetupForPlaza(motionLibrary, motionType)

  LoadAndSpawnPlaza(): view.SetCharacter(_lastSpawnedCharController) 추가

  SaveCurrentSessionToMockPlaza():
    newSession.signature_motion = session.signature_motion 추가


────────────────────────────────────────────────────────────────
[Unity Inspector 추가 작업]
────────────────────────────────────────────────────────────────

  GameFlowManager
    [ ] _signatureConfirmUI → SignatureMotionConfirmUI GameObject 연결

  PlazaManager
    [ ] characterAnimatorController → NPC_AnimatorController 연결
    [ ] motionLibrary → NPC_MotionLibrary.asset 연결


────────────────────────────────────────────────────────────────
[데이터 흐름 요약]
────────────────────────────────────────────────────────────────

  [가이드 모드]
  모션 입력 → 3초 재생 → 시그니처 확인 다이얼로그
    예 → SessionData.signature_motion = "Dance"
         Server Mode: PATCH /sessions/{id}/signature-motion
         Mock Mode: 메모리 저장 → 종료 시 mock_plaza.json 저장

  [자유 모드]
  PlazaManager.SpawnSessionAssets()
    → SetupCharacterForPlaza → PlacedCharacterController.SetupForPlaza(lib, Dance)

  플레이어 접근 → OnTriggerEnter → PlaySignatureMotion() → 루프 재생
  플레이어 이탈 → OnTriggerExit  → StopSignatureMotion() → idle 복귀


────────────────────────────────────────────────────────────────
[서버 요청 직렬화 방식 정리]
────────────────────────────────────────────────────────────────

  모든 PATCH 요청 본문: [Serializable] 클래스 + JsonUtility.ToJson() 통일
    BubbleTextRequest        → UpdateBubbleText
    SignatureMotionRequest   → SaveSignatureMotion

  GET 요청: 본문 없음
  POST LikeSession: 본문 없음 (세션 ID를 URL에 포함)


────────────────────────────────────────────────────────────────
[커밋 내역]
────────────────────────────────────────────────────────────────

  (이 커밋)


────────────────────────────────────────────────────────────────
[미해결 / 추후 작업]
────────────────────────────────────────────────────────────────

  [ ] SignatureMotionConfirmUI Unity 씬 구성 및 Inspector 연결
  [ ] PlazaManager Inspector: characterAnimatorController, motionLibrary 연결
  [ ] TriLib 설치 후 GeneratedCharacterSpawner.LoadFromServer() 구현
  [ ] Server Mode 전환 후 통합 테스트

================================================================
