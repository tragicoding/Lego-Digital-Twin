================================================================
Claude Code 작업 기록
날짜: 2026-06-01
브랜치: feature/unity
================================================================

────────────────────────────────────────────────────────────────
[세션 1] 히스토리 파일 분석 및 현재 브랜치 확인
────────────────────────────────────────────────────────────────

## 참고 문서
  DevelopmentHelp_0529.md
  Claude_0528.txt / Claude_0528_2.txt / Claude_0529.txt / Claude_0530.txt

## 현재 브랜치
  feature/unity (허용 브랜치, 정상)

## 파악한 이전 작업 현황 (0530 기준 미해결 항목)
  [ ] XR Origin "Player" 태그 설정 확인
  [ ] PlazaManager.spawnPoints 실제 광장 위치 배치
  [ ] 투표 중복 방지 로직 미구현
  [ ] GeneratedCharacterSpawner.LoadFromServer() TriLib 연동 미완


────────────────────────────────────────────────────────────────
[세션 2] 서버 모드 흐름 전체 점검
────────────────────────────────────────────────────────────────

## 점검 방법
  모든 스크립트 파일 직접 읽어 서버 데이터 클래스 필드별 연결 현황 추적

## 서버 데이터 클래스 필드 연결 현황 분석 결과

### SessionData
  | 필드              | 상태       |
  |-----------------|-----------|
  | session_id       | 연결됨     |
  | character_npc_name| 연결됨    |
  | bubble_text      | 연결됨     |
  | likes            | 연결됨     |
  | object_name      | 부분 연결  |
  | ready_for_unity  | 미사용 ⛔  |
  | assets.character | 연결됨     |
  | assets.object    | 연결됨     |

### CharacterAssetData
  | 필드         | 상태                          |
  |------------|------------------------------|
  | npc_name   | 연결됨                        |
  | model_url  | 경로 있으나 LoadFromServer TODO  |
  | texture_url| 미사용 ⛔ (코드 없음)            |
  | asset_id   | 미사용 (의도적)                 |
  | role       | 미사용 (의도적)                 |

### ObjectAssetData
  | 필드        | 상태         |
  |-----------|------------|
  | model_url | 연결됨       |
  | object_name| 부분 연결   |
  | asset_id  | 미사용 (의도적)|
  | role      | 미사용 (의도적)|

### PlazaSessionData
  | 필드                  | 상태              |
  |--------------------|-----------------|
  | session_id          | 연결됨            |
  | bubble_text         | 연결됨            |
  | likes               | 연결됨            |
  | is_top_liked        | 연결됨            |
  | assets.*.model_url  | 연결 끊김 ⛔       |
  | character_npc_name  | 미사용            |

### LikeResponse
  | 필드             | 상태          |
  |---------------|-------------|
  | session_id    | 연결됨        |
  | likes         | 연결됨        |
  | is_top_liked  | 연결됨        |
  | top_session_id| 미사용 ⛔     |

## 식별된 버그 6종
  1. WebSocketManager.StartListening() 미호출 → likes_updated 수신 불가 ⛔
  2. 오브제 비동기 타이밍 → _spawnedObjectGO null → AttachViewToCurrentSession 스킵 ⛔
  3. 캐릭터 서버 로드 미구현 (TriLib TODO) ⚠️
  4. 광장 배치 오브제 model_url 미사용 ⛔
  5. texture_url 미사용 ⛔
  6. ready_for_unity 미체크 ⛔


────────────────────────────────────────────────────────────────
[세션 3] 서버 연동 필드 연결 구현
────────────────────────────────────────────────────────────────

## 수정 파일 1: SessionManager.cs
  - LoadServerSession() 코루틴 추가
  - Server Mode에서 직접 FetchUnitySession 호출 → PollUntilReady → FetchUnitySession 체인으로 변경
  - ready_for_unity = true 확인 후 세션 데이터 로드

  변경 전:
    StartCoroutine(_apiClient.FetchUnitySession(sid, data => Apply(data, onLoaded)));

  변경 후:
    StartCoroutine(LoadServerSession(sid, onLoaded));

    private IEnumerator LoadServerSession(string sid, Action<SessionData> onLoaded)
    {
        yield return _apiClient.PollUntilReady(sid, null);
        yield return _apiClient.FetchUnitySession(sid, data => Apply(data, onLoaded));
    }


## 수정 파일 2: CharacterAnimationController.cs
  - Initialize()에 texture_url 처리 추가
  - ApplyTextureFromUrl() 구현
  - glTFast InstantiateMainSceneAsync 방식 사용 (GetMaterial은 glTFast 6.18에서 미지원)

  동작 방식:
    1. GltfImport.Load(texture_url) — GLB 비동기 로드
    2. 비활성 임시 컨테이너(_TempGltfMaterial)에 InstantiateMainSceneAsync
    3. Renderer.sharedMaterials 추출 → SkinnedMeshRenderer.materials 적용
    4. 임시 컨테이너 Destroy


## 수정 파일 3: PlazaManager.cs (3가지 변경)

  (1) SpawnSessionAssets — void → IEnumerator 변경
    - 오브제 model_url 있으면 SpawnObjectFromServerAsync() GLB 비동기 로드
    - 오브제 로드 실패 시 mockObjectPrefab fallback
    - 캐릭터는 TriLib 미구현 → 경고 로그 + mockCharacterPrefab fallback 유지
    - LoadAndSpawnPlaza 루프에서 yield return SpawnSessionAssets()로 변경

  (2) SpawnObjectFromServerAsync() 추가
    private async Task SpawnObjectFromServerAsync(sessionId, data, point)
      → GltfImport.Load(model_url)
      → 성공 시 root GameObject 생성 + InstantiateMainSceneAsync
      → 실패 시 mockObjectPrefab fallback

  (3) HandleServerLike(LikeResponse result) 추가
    → result.top_session_id 기준으로 전체 _views 별 표시 재계산
    → 해당 세션 뷰 UpdateLikes() 호출
    → _topSessionId 갱신


## 수정 파일 4: LikeSystem.cs
  - Server Mode SendLike() 응답 처리 변경
  변경 전: 개별 뷰만 UpdateLikes + SetTopLiked
  변경 후: PlazaManager.Instance.HandleServerLike(result) 위임
           → top_session_id 포함 전체 뷰 업데이트


────────────────────────────────────────────────────────────────
[세션 4] 버그 2종 수정
────────────────────────────────────────────────────────────────

## 버그 A — 오브제 비동기 타이밍 (GameFlowManager.cs)

  원인:
    OnSessionLoaded()에서 _objectSpawner.Spawn() 직후 즉시 GetSpawnedObject() 호출
    → Server Mode에서 GLB 비동기 로드가 끝나기 전 → null 반환
    → PlazaManager.RegisterCurrentSession(... null) → AttachViewToCurrentSession 스킵

  해결:
    OnGuideFinished()에서 참조 (가이드 시나리오 ~20초 후 GLB 로드 완료 시점)

    변경 전:
      plaza.RegisterCurrentSession(session, charGO, _spawnedObjectGO);

    변경 후:
      var objectGO = _spawnedObjectGO ?? _objectSpawner?.GetSpawnedObject();
      plaza.RegisterCurrentSession(session, charGO, objectGO);

    동작:
      - Mock Mode: _spawnedObjectGO 이미 설정됨 → ?? 좌변 반환
      - Server Mode: _spawnedObjectGO = null → GetSpawnedObject() 반환 (로드 완료본)


## 버그 B — WebSocket 미시작 (PlazaManager.cs)

  원인:
    EnterPlaza()에서 OnLikesUpdated += HandleLikesUpdated 만 있고
    WebSocketManager.StartListening() 호출 없음

  해결:
    EnterPlaza()에서 Server Mode 한정 StartListening() 호출

    if (SessionManager.Instance?.dataSourceMode == DataSourceMode.Server)
        WebSocketManager.Instance.StartListening();

    비고:
      - StartListening() 내부에 if (_running) return; 가드 있어 중복 호출 안전
      - Mock Mode에서는 불필요한 WebSocket 연결 방지


────────────────────────────────────────────────────────────────
[세션 5] 컴파일 에러 수정
────────────────────────────────────────────────────────────────

## 에러
  CS1061: 'GuideNPCController' does not contain a definition for 'OnFreeModeSwitched'
  → 이후 'OnPlayerTeleportRequested' 에러로 변경

## 원인 (cascade)
  CharacterAnimationController.cs 에서 gltf.GetMaterial(0) 호출
  → glTFast 6.18.0에서 GetMaterial() 메서드 미지원 → 컴파일 실패
  → CharacterAnimationController를 사용하는 GuideNPCController 컴파일 실패
  → GuideNPCController를 사용하는 GameFlowManager에서 이벤트 멤버를 못 찾는 에러

## 해결
  ApplyTextureFromUrl()에서 GetMaterial(0) → InstantiateMainSceneAsync 방식으로 변경
  (GeneratedObjectSpawner.cs에서 이미 검증된 API 사용)


────────────────────────────────────────────────────────────────
[최종 서버 모드 흐름 점검]
────────────────────────────────────────────────────────────────

  항목                               | 수정 전 | 수정 후
  ----------------------------------|--------|--------
  ready_for_unity 폴링               | ⛔     | ✅
  캐릭터 texture_url 적용             | ⛔     | ✅
  광장 이전 관람객 오브제 GLB 로드       | ⛔     | ✅
  LikeResponse.top_session_id 전체 반영| ⛔    | ✅
  오브제 비동기 타이밍 버그             | ⛔     | ✅
  WebSocket StartListening 미호출      | ⛔     | ✅
  캐릭터 FBX 서버 로드                 | ⛔     | ⚠️ TriLib 미설치 → Mock fallback


────────────────────────────────────────────────────────────────
[커밋 내역]
────────────────────────────────────────────────────────────────

  bf5322f  fix(unity): 서버 모드 데이터 연결 및 버그 수정


────────────────────────────────────────────────────────────────
[최종 파일 상태]
────────────────────────────────────────────────────────────────

## 수정
  | 파일                                    | 주요 변경 내용                                    |
  |----------------------------------------|------------------------------------------------|
  | Scripts/Managers/SessionManager.cs      | LoadServerSession() — PollUntilReady 체인        |
  | Scripts/Character/CharacterAnimationController.cs | ApplyTextureFromUrl() — texture_url 연결  |
  | Scripts/Managers/GameFlowManager.cs     | objectGO ?? GetSpawnedObject() — 비동기 타이밍 수정|
  | Scripts/Plaza/PlazaManager.cs           | SpawnObjectFromServerAsync, HandleServerLike,   |
  |                                        | WebSocket StartListening 추가                    |
  | Scripts/Plaza/LikeSystem.cs            | HandleServerLike 위임 — top_session_id 연결       |


────────────────────────────────────────────────────────────────
[미해결 / 추후 작업]
────────────────────────────────────────────────────────────────

  [ ] TriLib 설치 후 GeneratedCharacterSpawner.LoadFromServer() 구현
      → 가이드/배치 캐릭터 서버 FBX 런타임 로드
      → 광장 배치 캐릭터 서버 FBX 런타임 로드 (PlazaManager.SpawnSessionAssets)
  [x] XR Origin "Player" 태그 설정 확인 → 완료
  [ ] PlazaManager.spawnPoints 실제 광장 위치 배치
  [ ] 투표 중복 방지 로직 추가
  [ ] Server Mode 전환 후 통합 테스트


────────────────────────────────────────────────────────────────
[세션 6] 광장 이전 창작물 다양한 Mock 데이터 지원
────────────────────────────────────────────────────────────────

## 문제
  광장에 배치되는 이전 창작물들이 캐릭터/오브제 모두 동일한 단일 프리팹 사용
  → 모든 스폰 포인트에 같은 모양의 캐릭터·오브제 반복 등장

## 수정 파일 1: PlazaManager.cs
  - 단수 필드 → 배열로 변경
      mockCharacterPrefab  → mockCharacterPrefabs[]
      mockObjectPrefab     → mockObjectPrefabs[]
  - PickMockPrefab(prefabs[], index) 헬퍼 추가
      → index % prefabs.Length 순환 배정
  - SpawnSessionAssets(session, point, sessionIndex) — 인덱스 파라미터 추가
  - SpawnObjectFromServerAsync(sessionIndex) — fallback도 배열 방식 통일

## 수정 파일 2: Resources/Mock/mock_plaza.json
  - 세션 3개 → 5개 확장
  - 추가 세션: 하늘이(달빛 등대), 두루미(별똥별 로켓)

## Inspector 설정 (Unity Editor)
  PlazaManager → Mock Character Prefabs 배열에 npc_3, npc_9 등 연결
  PlazaManager → Mock Object Prefabs 배열에 여러 오브제 프리팹 연결
  세션 0→[0], 세션 1→[1], 세션 2→[0] ... 순환 배정


────────────────────────────────────────────────────────────────
[세션 7] 캐릭터·오브제 스폰 스케일 분리
────────────────────────────────────────────────────────────────

## 문제
  spawnScale 단일 값(12f)이 캐릭터·오브제 모두에 적용됨

## 수정 파일: PlazaManager.cs
  - spawnScale 제거 → 두 필드로 분리
      characterSpawnScale = 6f   (캐릭터)
      objectSpawnScale    = 12f  (오브제)
  - Mock/Server/fallback 모든 경로에 각각 적용


────────────────────────────────────────────────────────────────
[세션 8] 오브제 배치 방향 변경 (좌우 → 앞뒤)
────────────────────────────────────────────────────────────────

## 문제
  캐릭터가 왼쪽, 오브제가 오른쪽 배치 → 앞(캐릭터)/뒤(오브제) 배치로 변경 요청

## 수정 파일: PlazaManager.cs
  - Inspector 필드 추가
      characterForwardOffset = 3f  (캐릭터: spawnPoint 앞쪽)
      objectBackOffset       = 3f  (오브제: spawnPoint 뒤쪽)
  - 오프셋 방향 변경 (world space → spawnPoint local 기반)
      캐릭터: Vector3.left * 3f  → point.forward  * characterForwardOffset
      오브제: Vector3.right * 3f → -point.forward * objectBackOffset
  - point.forward 사용으로 spawnPoint 회전 방향 기준 앞/뒤 결정


────────────────────────────────────────────────────────────────
[세션 9] PlazaSessionView 뒤집힘 수정 (Billboard)
────────────────────────────────────────────────────────────────

## 문제
  PlazaSessionView(World Space Canvas)가 point.rotation을 그대로 물려받아
  카메라 반대 방향을 향해 UI가 뒤집혀 보임

## 수정 파일: Scripts/Plaza/PlazaSessionView.cs
  - _cam 필드 추가 (Camera 캐시)
  - Awake(): _cam = Camera.main
  - LateUpdate() 추가:
      transform.forward = _cam.transform.forward;
      → 매 프레임 카메라 forward로 정렬 (spawnPoint 회전 무관)
      → VR에서 플레이어 이동 시에도 항상 UI가 카메라 방향 유지


────────────────────────────────────────────────────────────────
[세션 10] 광장 배치 오브제 낙하 물리 구현
────────────────────────────────────────────────────────────────

## 요청
  오브제가 땅에 박혀 있음 → 하늘 위에서 중력으로 낙하시키기

## 수정 파일: PlazaManager.cs

  (1) Inspector 필드 추가
      objectDropHeight = 15f  (스폰 높이 — 낙하 시작 위치)

  (2) 오브제 스폰 위치 변경
      변경 전: point.position - point.forward * objectBackOffset
      변경 후: point.position - point.forward * objectBackOffset + Vector3.up * objectDropHeight

  (3) SetupObjectPhysics(GameObject go) 추가
      - Rigidbody: useGravity = true, FreezeRotation (쓰러짐 방지)
      - 기존 Collider 없을 때 자식 MeshFilter마다 MeshCollider(convex) 자동 추가
      - Mock/Server GLB/fallback 세 경로 모두 호출

  주의: 씬 바닥에 Collider 있어야 정상 착지


────────────────────────────────────────────────────────────────
[세션 11] MissingComponentException 수정
────────────────────────────────────────────────────────────────

## 에러
  MissingComponentException: There is no 'Rigidbody' attached to the
  "PlazaObj_plaza_001" game object
  LegoTwin.Plaza.PlazaManager.SetupObjectPhysics() 에서 발생

## 원인
  Unity Object에 C# ?? 연산자 사용
  → ??는 ReferenceEquals 기반 → Unity null 오브젝트를 null로 인식 못 함
  → GetComponent<Rigidbody>()가 Unity null 반환해도 ??가 AddComponent를 건너뜀
  → null Rigidbody에 .useGravity 접근 → 예외 발생

## 해결
  ?? 연산자 → if (rb == null) 패턴으로 교체 (Unity == 오버로드 활용)

  변경 전:
    var rb = go.GetComponent<Rigidbody>() ?? go.AddComponent<Rigidbody>();
    var col = mf.gameObject.GetComponent<MeshCollider>() ?? mf.gameObject.AddComponent<MeshCollider>();

  변경 후:
    var rb = go.GetComponent<Rigidbody>();
    if (rb == null) rb = go.AddComponent<Rigidbody>();
    var col = mf.gameObject.GetComponent<MeshCollider>();
    if (col == null) col = mf.gameObject.AddComponent<MeshCollider>();

## 수정 파일
  Scripts/Plaza/PlazaManager.cs


────────────────────────────────────────────────────────────────
[커밋 내역 — 2차]
────────────────────────────────────────────────────────────────

  46e91c6  feat(unity): 광장 이전 창작물 다양화·배치 개선·낙하 물리 구현


────────────────────────────────────────────────────────────────
[세션 12] 앱 종료 시 현재 세션 mock_plaza.json 자동 저장
────────────────────────────────────────────────────────────────

## 요청
  Mock 모드에서 관람객이 나갈 때 현재 세션이 mock_plaza.json에 추가되어
  다음 관람객에게 이전 창작물로 표시되도록 구현

## 수정 파일: PlazaManager.cs

  OnApplicationQuit() 추가
    → Mock 모드일 때만 SaveCurrentSessionToMockPlaza() 호출

  SaveCurrentSessionToMockPlaza() 추가 (public — 수동 호출도 가능)
    1. _currentSession ?? SessionManager.Instance.CurrentSession 참조
    2. Application.dataPath 기반 절대 경로로 mock_plaza.json 읽기
    3. 동일 session_id 중복 방지 (같은 세션 두 번 저장 방지)
    4. 기존 plaza_XXX 최대 번호 + 1로 새 ID 생성 (plaza_006, plaza_007, ...)
    5. PlazaSessionData 생성 (likes = 0)
    6. top_session_id 재계산 후 파일 저장

  주의: Application.dataPath 경로는 Editor/개발 빌드 전용
        Server 모드에서는 OnApplicationQuit에서 즉시 return

## 흐름
  앱 종료 → OnApplicationQuit → Mock 모드 확인
    → mock_plaza.json 읽기 → 현재 세션 추가 → 파일 저장
    → 다음 관람객 입장 시 광장에 표시됨


────────────────────────────────────────────────────────────────
[세션 13] 광장 최대 31개 제한 + likes 상위 8위 보호 로직
────────────────────────────────────────────────────────────────

## 요청
  광장 최대 31개 창작물 배치. 초과 시 제거 기준:
    - 오래된 것부터 제거 (plaza_XXX 번호 낮은 순)
    - 단, likes 상위 8위는 제거 대상에서 보호

## 수정 파일: PlazaManager.cs

  상수 추가
    private const int MaxPlazaSessions = 31;

  SaveCurrentSessionToMockPlaza() 내 트리밍 로직 추가
    1. likes 상위 8개 session_id → protectedIds HashSet
    2. protectedIds 제외 세션 중 plaza_XXX 번호 가장 낮은 것 선택 → 제거
    3. 예외: 모두 상위 8위인 경우 → 보호 없이 가장 오래된 것 제거

  PlazaSessionNumber(sessionId) 헬퍼 추가
    → "plaza_XXX"에서 숫자 추출 (정렬·비교용)

## 예시
  plaza_001 (likes 2, 비보호) → 제거 대상
  plaza_002 (likes 15, 상위 8위) → 보호, 건너뜀
  plaza_003 (likes 0, 비보호) → plaza_001 제거 후 다음 후보


────────────────────────────────────────────────────────────────
[세션 14] Mock 세션 풀 — 관람객마다 다른 캐릭터/오브제
────────────────────────────────────────────────────────────────

## 문제
  mock_session.json이 정적 파일이라 매번 같은 캐릭터(몽글이)·오브제(하늘탑)로
  고정됨 → 관람객마다 다른 세션 데이터 필요

## 구현

  신규 파일: Resources/Mock/mock_session_pool.json
    - 8개 세션 정의 (몽글이, 별이, 뭉게, 토실, 하늘이, 두루미, 초코, 루나)
    - 각 세션마다 다른 캐릭터명·오브제명·말풍선 텍스트

  수정 파일: Scripts/Mock/MockSessionLoader.cs
    - SessionPool 내부 클래스 추가 (List<SessionData> sessions)
    - PlayerPrefs("MockSessionIndex") 로 순환 인덱스 관리
    - Load() 우선순위:
        1. mock_session_pool.json 로드 → sessions[index % count] 반환
           PlayerPrefs index 1 증가 후 저장
        2. mock_session.json fallback (풀 없을 시)
    - 로그: [MockSessionLoader] 풀 로드 [1/8] — character: 몽글이, object: 하늘탑

## 순환 방식
  1회차: index=0 → pool[0] 몽글이  →  index 1 저장
  2회차: index=1 → pool[1] 별이    →  index 2 저장
  ...
  9회차: index=8 → pool[0] 몽글이  →  순환 반복

## 비고
  - Server 모드: MockSessionLoader 호출 자체 없음 → 영향 없음
  - 인덱스 초기화: Unity Editor → Edit → Clear All PlayerPrefs
  - 풀 세션 추가: mock_session_pool.json에 항목만 추가 (코드 수정 불필요)


────────────────────────────────────────────────────────────────
[커밋 내역 — 3차]
────────────────────────────────────────────────────────────────

  bd58dc0  feat(unity): Mock 세션 풀 순환·plaza 자동 저장·31개 제한 구현


────────────────────────────────────────────────────────────────
[세션 15] 투표 UI 높이 수정 (바닥 밑 노출 문제)
────────────────────────────────────────────────────────────────

## 문제
  PlazaSessionView가 spawnPoint.position(바닥)에 그대로 생성되어 UI가 땅 아래로 가라앉음

## 수정 파일: PlazaManager.cs
  - Inspector 필드 추가: viewHeightOffset = 5f
  - 이전 관람객 뷰:
      point.position + Vector3.up * viewHeightOffset
      → new Vector3(point.position.x, viewHeightOffset, point.position.z)  (절대 Y 기준)
  - 현재 관람객 뷰(AttachViewToCurrentSession):
      _currentObjectGO.transform.position + viewHeightOffset
      → new Vector3(objPos.x, viewHeightOffset, objPos.z)  (절대 Y 기준)
  - 두 경우 모두 절대 Y = viewHeightOffset 로 통일하여 높이 일치


────────────────────────────────────────────────────────────────
[세션 16] PlazaSessionView Billboard 카메라 설정 (마우스 클릭 대응)
────────────────────────────────────────────────────────────────

## 문제
  World Space Canvas 버튼 마우스 클릭 불가
  → VR 프로젝트에서 EventSystem 입력 모듈이 XR 전용으로 교체되어 마우스 처리 안 됨

## 수정 파일 1: PlazaSessionView.cs
  - Start() 추가: 자식 Canvas 전체에 worldCamera = Camera.main 자동 설정
    (World Space Canvas는 Event Camera 설정 필요)

## 수정 파일 2: LikeSystem.cs
  - using UnityEngine.UI 추가
  - _cam, _likeButtonRect 필드 추가
  - Start(): Button RectTransform 자동 캐시
  - Update() 추가: RectTransformUtility.RectangleContainsScreenPoint()
    마우스 클릭 위치가 버튼 Rect 안에 있으면 OnLikePressed() 직접 호출
    → EventSystem 없이도 마우스 클릭 동작


────────────────────────────────────────────────────────────────
[세션 17] 투표 중복 방지 + 안내 메시지
────────────────────────────────────────────────────────────────

## 요청
  - 여러 오브제에 하트 가능 (중복 X)
  - 같은 오브제 재투표 시 안내 메시지 표시

## 수정 파일: LikeSystem.cs
  - static HashSet<string> _likedSessionIds 추가
    → 모든 LikeSystem 인스턴스가 공유 (앱 재시작 시 자동 초기화)
  - alreadyLikedUI 필드 추가 (Inspector 연결)
  - alreadyLikedDisplayDuration 필드 추가 (기본 2초)
  - OnLikePressed():
      _likedSessionIds.Contains → ShowAlreadyLikedMessage() 호출 후 return
  - SendLike(): 성공 시 _likedSessionIds.Add(_sessionId)
  - ShowAlreadyLikedMessage(): alreadyLikedUI 2초 표시 후 자동 숨김
  - OnTriggerExit: alreadyLikedUI도 함께 숨김

## Inspector 설정
  LikeSystem → Already Liked UI 슬롯에 안내 텍스트 UI 연결


────────────────────────────────────────────────────────────────
[세션 18] FreeCameraController 신규 생성 (씬 뷰 방식 이동)
────────────────────────────────────────────────────────────────

## 생성 파일: Scripts/Core/FreeCameraController.cs

## 기능
  마우스 오른쪽 버튼 + 드래그 — 시선 회전 (Pitch/Yaw)
  마우스 오른쪽 버튼 + WASD  — 전후좌우 이동
  마우스 오른쪽 버튼 + Q/E   — 위/아래 이동
  Shift                       — 이동 속도 증가 (fastMoveSpeed)
  마우스 스크롤               — 앞뒤 빠른 이동

## 설계
  - 오른쪽 버튼 누르는 동안만 이동/회전 활성화
    → 버튼 마우스 클릭과 충돌 없음
  - 오른쪽 버튼 해제 시 Cursor 복원

## Inspector 필드
  moveSpeed = 10f, fastMoveSpeed = 30f, scrollSpeed = 20f
  mouseSensitivity = 2f

## 사용법
  Main Camera (또는 카메라를 담은 GameObject)에 컴포넌트 추가
  VR 배포 시 비활성화


────────────────────────────────────────────────────────────────
[세션 19] 가이드 모드 중 FreeCameraController 이동 잠금
────────────────────────────────────────────────────────────────

## 요청
  가이드 모드에서는 카메라 이동 불가, 고개(시선 회전)만 가능

## 수정 파일 1: GameFlowManager.cs
  - public static bool IsGuideMode { get; private set; } = true 추가
  - OnGuideFinished(): IsGuideMode = false 설정

## 수정 파일 2: FreeCameraController.cs
  - using LegoTwin.Managers 추가
  - WASD 이동·스크롤 블록을 if (!GameFlowManager.IsGuideMode) 로 감쌈
  - 가이드 모드 중: 마우스 회전만 동작
  - 자유 모드 전환(OnGuideFinished) 시: 이동 자동 활성화


────────────────────────────────────────────────────────────────
[커밋 내역 — 4차]
────────────────────────────────────────────────────────────────

  (커밋 예정)


────────────────────────────────────────────────────────────────
[최종 파일 상태 업데이트]
────────────────────────────────────────────────────────────────

## 수정
  | 파일                                 | 주요 변경 내용                                               |
  |-------------------------------------|-------------------------------------------------------------|
  | Scripts/Plaza/PlazaManager.cs       | 배열 Prefab, 스케일 분리, 앞뒤 배치, 낙하 물리, ?? 버그 수정,  |
  |                                     | OnApplicationQuit 저장, 31개 제한·보호 로직, UI 높이 수정      |
  | Scripts/Plaza/PlazaSessionView.cs   | Billboard, Start()에서 worldCamera 자동 설정                  |
  | Scripts/Plaza/LikeSystem.cs         | 마우스 클릭 감지, 중복 방지, 안내 메시지                        |
  | Scripts/Managers/GameFlowManager.cs | IsGuideMode 정적 플래그 추가                                  |
  | Scripts/Mock/MockSessionLoader.cs   | SessionPool 순환 로드                                        |
  | Resources/Mock/mock_plaza.json      | 세션 5개 확장                                                |

## 신규 생성
  | 파일                                       | 내용                               |
  |-------------------------------------------|------------------------------------|
  | Scripts/Core/FreeCameraController.cs       | 씬 뷰 방식 자유 카메라 컨트롤러       |
  | Resources/Mock/mock_session_pool.json      | 8개 세션 풀 (관람객마다 순환)         |


────────────────────────────────────────────────────────────────
[미해결 / 추후 작업]
────────────────────────────────────────────────────────────────

  [ ] TriLib 설치 후 GeneratedCharacterSpawner.LoadFromServer() 구현
  [x] PlazaManager.spawnPoints 실제 광장 위치 배치 완료
  [ ] Server Mode 전환 후 통합 테스트
  [ ] 백엔드에 광장 세션 정렬 정책 요청
      (최대 표시 수, 우선순위 기준 — 현재 spawnPoints 수로만 제한됨)

================================================================


================================================================
Claude Code 작업 기록 (2차)
날짜: 2026-06-01
브랜치: feature/unity
================================================================

────────────────────────────────────────────────────────────────
[세션 20] 투표 UI — likeButtonUI → likePanel 로 변경
────────────────────────────────────────────────────────────────

## 수정 파일: Scripts/Plaza/LikeSystem.cs
  - likeButtonUI 필드 → likePanel 로 전체 rename
  - Tooltip 변경: "근접 시 표시할 좋아요 Panel (비활성으로 시작)"
  - 버튼이 아닌 Panel 전체가 비활성 → 활성 토글되도록 개념 변경

## Inspector
  LikeSystem → Like Panel 슬롯에 Panel GameObject 연결


────────────────────────────────────────────────────────────────
[세션 21] PlazaSessionView Billboard 제거
────────────────────────────────────────────────────────────────

## 수정 파일: Scripts/Plaza/PlazaSessionView.cs
  - LateUpdate() 빌보드 코드 제거 (transform.forward = _cam.transform.forward)
  - UI가 스폰된 방향 그대로 고정됨


────────────────────────────────────────────────────────────────
[세션 22] UI 회전 보정
────────────────────────────────────────────────────────────────

## 문제
  PlazaSessionView가 스폰 시 반대 방향을 향함

## 수정 파일: Scripts/Plaza/PlazaManager.cs
  - 이전 작품: point.rotation * Quaternion.Euler(0f, 180f, 0f)
  - 현재 작품: _currentObjectGO.transform.rotation * Quaternion.Euler(0f, 180f, 0f)

## PlazaSessionView.cs
  - Awake()에서 transform.Rotate(0f, 180f, 0f) 시도 → 제거
  - 최종적으로 PlazaManager 두 곳에서 각각 보정 적용


────────────────────────────────────────────────────────────────
[세션 23] LikeSystem triggerRadius → SphereCollider 자동 적용
────────────────────────────────────────────────────────────────

## 문제
  triggerRadius 필드 선언만 되고 SphereCollider.radius에 미적용 → 인식 거리 좁음

## 수정 파일: Scripts/Plaza/LikeSystem.cs
  - Start()에서 GetComponent<SphereCollider>().radius = triggerRadius 추가
  - 기본값 2f → 8f 로 변경
  - Inspector Trigger Radius 값 변경 시 콜라이더 크기도 함께 변경됨


────────────────────────────────────────────────────────────────
[세션 24] SendLike 후 패널 유지
────────────────────────────────────────────────────────────────

## 수정 파일: Scripts/Plaza/LikeSystem.cs
  - SendLike() 시작 시 likePanel.SetActive(false) 제거
  - 버튼 클릭 후 패널이 유지되고, 범위 벗어날 때만 사라짐


────────────────────────────────────────────────────────────────
[세션 25] Mock likes 파일 저장 구현
────────────────────────────────────────────────────────────────

## 문제
  HandleMockLike가 메모리(_likeCounts)만 갱신하고 mock_plaza.json에 저장 안 됨

## 수정 파일: Scripts/Plaza/PlazaManager.cs

  SaveMockLikesToFile(sessionId, newLikes, topSessionId) 추가
    - mock_plaza.json 읽기 → 해당 세션 likes 갱신 → is_top_liked 재계산 → 파일 저장
    - try-catch로 파일 쓰기 실패 에러 로그
    - UNITY_EDITOR에서 AssetDatabase.Refresh() 호출 → IDE 즉시 반영

  HandleMockLike()에서 SaveMockLikesToFile() 호출 추가

  파일 경로 분기 (MockPlazaFilePath):
    - UNITY_EDITOR: Application.dataPath + "00_Project/Resources/Mock/mock_plaza.json"
      (IDE에서 즉시 확인 가능)
    - 빌드: Application.persistentDataPath + "mock_plaza.json"
      (읽기 전용 번들 우회, 쓰기 가능)
    - EnsureMockPlazaFile(): 빌드에서만 Resources → persistentDataPath 복사

  현재 세션 처리:
    - 현재 세션(pool_XXX)은 파일에 없으므로 SaveMockLikesToFile에서 조용히 스킵
    - SaveCurrentSessionToMockPlaza에서 플레이 중 받은 likes 수 반영
      변경 전: likes = 0
      변경 후: _likeCounts.TryGetValue(session.session_id, out int currentLikes)


────────────────────────────────────────────────────────────────
[커밋 내역]
────────────────────────────────────────────────────────────────

  (커밋 예정)


────────────────────────────────────────────────────────────────
[최종 파일 상태]
────────────────────────────────────────────────────────────────

## 수정
  | 파일                               | 주요 변경 내용                                          |
  |-----------------------------------|--------------------------------------------------------|
  | Scripts/Plaza/LikeSystem.cs       | likePanel rename, triggerRadius→Collider, 패널 유지     |
  | Scripts/Plaza/PlazaManager.cs     | UI 회전 보정, SaveMockLikesToFile, 파일 경로 분기        |
  | Scripts/Plaza/PlazaSessionView.cs | Billboard 제거, Awake 회전 보정                         |
  | Resources/Mock/mock_plaza.json    | likes 수 업데이트                                       |
  | Assets/Prefabs/PlazaSessionView.prefab | StarObject 비활성화 설정                          |


────────────────────────────────────────────────────────────────
[미해결 / 추후 작업]
────────────────────────────────────────────────────────────────

  [ ] TriLib 설치 후 GeneratedCharacterSpawner.LoadFromServer() 구현
  [ ] Server Mode 전환 후 통합 테스트
  [ ] 백엔드에 광장 세션 정렬 정책 요청

================================================================
