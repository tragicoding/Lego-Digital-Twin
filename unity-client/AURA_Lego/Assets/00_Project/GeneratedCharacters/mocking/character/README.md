# Mock Character

이 폴더에 리깅된 FBX 파일을 넣으세요.

## 사용법
1. 리깅된 캐릭터 FBX 파일을 이 폴더에 복사
2. Unity가 자동으로 임포트하면 Prefab 생성
3. GeneratedCharacterSpawner (Inspector) 의 `mockCharacterPrefab` 필드에 드래그

## 서버 모드 (전시 시)
- 서버에서 FBX URL이 내려오면 런타임 다운로드 후 자동 교체
- 이 폴더의 파일은 Mock Mode 전용
