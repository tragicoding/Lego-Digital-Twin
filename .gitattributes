# ====================================================================
# Unity 협업용 Git 속성 — 씬/프리팹 충돌 자동 병합 + 바이너리 보호
# (UnityYAMLMerge 등록 방법: unity-client/AURA_Lego/Assets/00_Project/History/UNITY_COLLABORATION.md 참고)
# ====================================================================

# Unity YAML 텍스트 에셋 → UnityYAMLMerge(Smart Merge)로 3-way 병합
*.unity          merge=unityyamlmerge
*.prefab         merge=unityyamlmerge
*.asset          merge=unityyamlmerge
*.mat            merge=unityyamlmerge
*.anim           merge=unityyamlmerge
*.controller     merge=unityyamlmerge
*.overrideController merge=unityyamlmerge
*.meta           merge=unityyamlmerge
*.physicMaterial merge=unityyamlmerge

# 바이너리 에셋 → diff/머지 금지 (잘못된 텍스트 병합·손상 방지)
*.fbx          binary
*.png          binary
*.PNG          binary
*.jpg          binary
*.jpeg         binary
*.tga          binary
*.psd          binary
*.glb          binary
*.gltf         binary
*.wav          binary
*.mp3          binary
*.ogg          binary
*.dll          binary
*.a            binary
*.so           binary
*.unitypackage binary
